const name = "My Name";
const normal_greeting = "Hello " + name;
const template_greeting = `Hello ${name}`;

console.log(normal_greeting);
console.log(template_greeting);

// Get current year and print it using template-literal
console.log(`The current year is ${new Date().getFullYear()}`);
